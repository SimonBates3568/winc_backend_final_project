import { Router } from "express";

const router = Router();

//GET /amenities => returns all amenities

// POST /amenities => create a new amenity(JWT TOKEN AUTHENTICATION REQUIRED!)
// GET /amenities/:id => get amenity by id
// PUT /amenities/:id => update amenity by id(JWT TOKEN AUTHENTICATION REQUIRED!)
// DELETE /amenities/:id => delete amenity by id(JWT TOKEN AUTHENTICATION REQUIRED!)






export default router;